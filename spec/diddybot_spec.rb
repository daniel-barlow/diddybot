# frozen_string_literal: true

RSpec.describe Diddybot do
  it "has a version number" do
    expect(Diddybot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
